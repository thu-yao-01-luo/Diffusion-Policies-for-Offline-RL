_MACROS:
  DATA_DIR: /cephfs/xcw/data
  ASSET_DIR: /cephfs/xcw/dreamfuser/assets
  VISION_TASK_PATH: /cephfs/xcw/dreamfuser/assets/TASK
  MODEL_DIR: /cephfs/xcw/dreamfuser/models
  RENDER_DEVICES: 0
  
_CMDS:
  - wandb login ${oc.env:WANDB_API_KEY}
  - cp /cephfs/xcw/dreamfuser/nvidia_icd.json /etc/vulkan/icd.d/nvidia_icd.json # renderer
  - git pull origin caiwei_branch
  - git checkout caiwei_branch
